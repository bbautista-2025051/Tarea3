# Copiar este archivo a backend/.env y ajustar los valores segun tu entorno local.
# El backend lee las variables de entorno desde backend/.env

# Puerto en el que correra el servidor Express
PORT=3000

# Configuracion de PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aplicacion_clientes
DB_USER=postgres
DB_PASSWORD=admin
